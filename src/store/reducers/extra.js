// import { INCREMENT_ID } from "../actionTypes"

const initialState = {
    extra: 'extra',
}

export default function(state = initialState, action) {
    switch (action.type) {
      default:
        return state;
    }
}